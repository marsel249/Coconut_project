'''- **Задание #1**
    1. Создайте переменные `name` со значением `"Дмитрий"` и `age` со значением `28`.
    2. Вставьте значения переменных `name` и `age` в строку с помощью `f-строки`, и сохраните результат в переменную `greeting_f`.
Проверьте результат.
- **Задание #2**
    1. Создайте переменные `city` со значением `"Москва"` и `temperature` со значением `15.5`.
    2. Вставьте значения переменных `city` и `temperature` в строку с помощью метода `.format()`,
и сохраните результат в переменную `weather_format`. Проверьте результат.
- **Задание #3**
    1. Создайте переменные `product` со значением `"ноутбук"` и `price` со значением `54999`.
    2. Вставьте значения переменных `product` и `price` в строку с помощью оператора `%`, и
сохраните результат в переменную `product_info_percent`. Проверьте результат.'''

# name = "Дмитрий"
# age = 28
# greeting_f = f'{name} {age}'
# print(greeting_f)

# city = "Москва"
# temperature = 15.5
# weather_format = 'llalalal {}, {} '.format(city, temperature)
# print(weather_format)

# product = "ноутбук"
# price = 54999
# product_info_percent = '%s, %s' % (product, price)
# print(product_info_percent)



