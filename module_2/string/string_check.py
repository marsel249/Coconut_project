'''Основные методы:
* in оператор: проверяет наличие подстроки в строке.
* startswith(): проверяет, начинается ли строка с определённой подстроки.
* endswith(): проверяет, заканчивается ли строка на определённую подстроку.
* isdigit(): проверяет, состоит ли строка только из цифр.
* isalnum(): проверяет, состоит ли строка только из букв и цифр.
* isspace(): проверяет, состоит ли строка только из пробелов.
* Задание#6
    * Создайте переменную phrase со значением "The quick brown fox jumps over the lazy dog".
    * Проверьте, содержит ли phrase слово "fox", используя оператор in.
Сохраните результат в переменную contains_fox. Проверьте результат.
* Задание#6.1
    * Создайте переменную greeting со значением "Hello, world!".
    * Используйте метод startswith() для проверки, начинается ли greeting с "Hello".
Сохраните результат в переменную starts_with_hello. Проверьте результат.
    * Используйте метод endswith() для проверки, заканчивается ли greeting на "world!".
Сохраните результат в переменную ends_with_world. Проверьте результат.
* Задание#6.2
    * Создайте переменную text со значением "12345".
    * Проверьте, состоит ли text только из цифр, используя метод isdigit(). Сохраните результат в переменную is_digits.
Проверьте результат.
    * Измените значение text на "abc123". Проверьте is_digits ещё раз, чтобы увидеть, что изменилось.
* Задание#6.3
    * Создайте переменную mixed_text со значением "Hello123".
    * Используйте метод isalnum() для проверки, состоит ли mixed_text только из букв и цифр (без пробелов или специальных символов).
Сохраните результат в переменную is_alphanumeric. Проверьте результат.
* Задание#6.4
    * Создайте переменную whitespace_text со значением " ".
    * Проверьте, состоит ли whitespace_text только из пробелов, используя метод isspace().
Сохраните результат в переменную is_space. Проверьте результат'''

# phrase = 'The quick brown fox jumps over the lazy dog'
# word = 'fox'
# contains_fox = word in phrase
# print(contains_fox)
# print(word in phrase)

# reeting = "Hello, world!"
# starts_with_hello = reeting.startswith('Hello')
# print(starts_with_hello)
# ends_with_world = reeting.endswith('world!')
# print(ends_with_world)

# text = "12345"
# is_digits = text.isdigit()
# print(is_digits)
# text = "abc12345"
# is_digits = text.isdigit()
# print(is_digits)

# ixed_text = "Hello123"
# is_alphanumeric = ixed_text.isalnum()
# print(is_alphanumeric)

# whitespace_text = ' '
# is_space = whitespace_text.isspace()
# print(is_space)

